// This crate is currently empty as we're using declarative macros instead.
// The #[replicated] macro is defined in the main lib.rs as a macro_rules! macro
// because Rust doesn't yet support custom attributes on statements (it's unstable).
